{
  "name": "commander-canvas-github-pages",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Static GitHub Pages Commander deck builder with a browser-cached Scryfall card database",
  "scripts": {
    "dev": "node dev-server.mjs",
    "start": "node dev-server.mjs",
    "build:data": "node scripts/build-card-db.mjs",
    "check": "node --check public/app.js && node --check public/service-worker.js && node --check scripts/build-card-db.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
